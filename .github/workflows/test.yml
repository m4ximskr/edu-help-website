name: Test

on:
  workflow_dispatch: # Putting here is also fine!!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v1
      - name: Branch name
        run: echo ${{ github.ref == 'refs/heads/master' && 'ms' || 'dev' }}
